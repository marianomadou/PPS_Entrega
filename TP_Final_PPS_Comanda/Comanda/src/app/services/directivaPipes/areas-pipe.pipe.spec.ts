import { AreasPipePipe } from './areas-pipe.pipe';

describe('AreasPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AreasPipePipe();
    expect(pipe).toBeTruthy();
  });
});
