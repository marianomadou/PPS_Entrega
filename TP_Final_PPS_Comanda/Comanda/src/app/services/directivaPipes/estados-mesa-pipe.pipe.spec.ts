import { EstadosMesaPipePipe } from './estados-mesa-pipe.pipe';

describe('EstadosMesaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EstadosMesaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
